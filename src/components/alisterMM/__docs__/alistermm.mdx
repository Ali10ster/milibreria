import { Canvas, Meta } from "@storybook/blocks";
import example from "./example.tsx";
import * as AlisterMM from "./alistermm.stories.tsx";

<Meta of={AlisterMM} title="AlisterMM" />

# AlisterMM Componente de tipografía

#### Ejemplo

<Canvas of={AlisterMM.Primary} />

## Uso

```ts
import { AlisterMM } from 'milibreria';

function Example(){
    return (
        <AlisterMM
            text="Componente AlisterMM primario",
            alignment="center",
            color="success",
            bottomMargin= true,
            variant="h1",
        />
    );
};

export default Example;

<Canvas of={AlisterMM.Primary} />

## Uso

```ts
import { AlisterMM } from 'milibreria';

function Example(){
    return (
        <AlisterMM
            text="Componente AlisterMM secundario",
            alignment="left",
            color="warning",
            bottomMargin=false,
            variant="h3",
        />
    );
}

